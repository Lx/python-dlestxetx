dist: xenial   # required for Python >= 3.7
language: python
python:
  - '3.6'
  - '3.7'
install:
  - pip install coveralls
script:
  - coverage run --source=dlestxetx setup.py test
after_success:
  - coveralls
